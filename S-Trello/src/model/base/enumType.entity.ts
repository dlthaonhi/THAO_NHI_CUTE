export enum RoleType {
  ADMIN = "admin",
  MEMBER = "member",
}

export enum VisibilityType {
  WORKSPACE = "workspace", // all members in the workspace/ project
  PRIVATE = "private" // only the creater member is the admin
}
